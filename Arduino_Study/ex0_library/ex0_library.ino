// 1. 라이브러리 사용 순서
  // 1way) Arduino IDE
    // IDE -> 상단 스케치 -> 라이브러리 포함하기 -> 라이브러리 관리 -> 검색 후 설치
  // 2way) 구글링
    // 1way) 라이브러리 다운 -> IDE -> 상단 스케치 -> 라이브러리 포함하기 -> .ZIP 라이브러리 추가 -> 라이브러리 파일 추가
    // 2way) Arduino IDE 오른쪽 마우스 -> 속성 -> 바로가기 -> 파일 위치 열기 -> libraries 폴더에 복붙
  // 3way) github
    // 라이브러리 다운 -> IDE -> 상단 스케치 -> 라이브러리 포함하기 -> .ZIP 라이브러리 추가 -> 라이브러리 파일 추가
